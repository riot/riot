<escape-in-expression>

  <p title={ "This is a title" }>Hover me.</p>
  <p if={ a == "a" }>It should be shown.</p>
  <p class={ red: a == "a" }>It should be red.</p>

  <style scoped>
    .red { color: red }
  </style>

  this.a = 'a'

</escape-in-expression>
